# Notes Studio

A simple GitHub Pages-style note workspace with:

- a sidebar for managing different sections
- notes inside each section
- checklist-style lists
- optional scoring for list items with a configurable max score

Open [index.html](index.html) directly in a browser, or publish the repository as a GitHub Pages site from the root folder.
